<% if @user.is_admin %>
  <% @attractions.each do |a| %>
    <%= link_to "Show #{a.name}", attraction_path(a) %>
    <%= link_to "Edit #{a.name}", edit_attraction_path(a) %>
  <% end %>
  <%= link_to "New Attraction", new_attraction_path %>
<% else %>
  <% @attractions.each do |a| %>
    <%= link_to "Go on #{a.name}", attraction_path(a) %>
  <% end %>
<% end %>